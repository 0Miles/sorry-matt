interface:
  display_name: "Review PR"
  short_description: "Publish evidenced GitHub PR feedback"
  default_prompt: "Use $review-pr to review this GitHub PR and publish verified feedback."
policy:
  allow_implicit_invocation: false
